/* jquery.tabs.js - jQuery 탭 플러그인, 2012 © yamoo9.com
---------------------------------------------------------------- */
;(function($) { 
	$.fn.tabs = function(options) {
		// 옵션 설정.
		options = $.extend({

		}, options);
		// jQuery 체인.
		return this.each(function() {
			
			
			
		}); // end: return
	}; //end: plug-in
})(jQuery);